name: go-fw

env:
  name: local
  type: local
log:
  level: debug

httpServer:
  port: 8080
  bindAddress:
  basePath: /api/v1/
  reqLoggerExcludes: [ "/health", "/ready" ]

database:
  user: postgres
  pass: postgres
  host: localhost
  port: 5432

redis:
  url: redis://default:pass@localhost:6379/0